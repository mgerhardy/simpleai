To compile this on a debian based linux, you will need the following packages
* qt5-default

You will need to add the Ubuntu SDK with Qt and QML tools executing the following statements:
sudo add-apt-repository ppa:ubuntu-sdk-team/ppa 
